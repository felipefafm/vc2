import { Button } from "react-bootstrap"

const CartWidget =()=>{
    return(
        <Button>
            <img src ={'./images/cart.svg'} alt='cart-widget'/>
            0
        </Button>
    )
}
export default CartWidget